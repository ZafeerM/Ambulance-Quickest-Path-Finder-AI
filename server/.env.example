# Copy this file to .env and adjust values as needed (template for orignal ENV)

# Seconds to pause between each A* step streamed over WebSocket (0 = no delay)
STEP_DELAY_SECONDS=0.1

# Comma-separated list of allowed CORS origins
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
